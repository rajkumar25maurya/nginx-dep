apiVersion: v1
kind: Service
metadata:
  name: nginx-frontend-service
  namespace: nginx-frontend
spec:
  type: NodePort
  selector:
    app: nginx
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30084 